# --- Terraform ---
# Local .terraform directories (provider plugins, modules cache)
**/.terraform/*
**/.terraform

# State files - may contain secrets and are environment-specific
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform.tfstate.lock.info

# Crash log files
crash.log
crash.*.log

# Variable files that may contain secrets (passwords, tokens).
# Commit sanitized examples as *.tfvars.example instead.
*.tfvars
*.tfvars.json
!*.tfvars.example

# Terraform override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan output
*.tfplan

# CLI configuration / credentials
.terraformrc
terraform.rc

# --- Secrets / tokens (NEVER commit these) ---
env.sh
**/env.sh
*.env
.env
secrets.auto.tfvars

# Terraform dependency lock file (ignored per user preference; note: committing
# it is generally recommended for reproducible provider versions).
.terraform.lock.hcl
**/.terraform.lock.hcl

# --- Generated unattended-install artifacts (contain plaintext credentials) ---
# The rendered autounattend.xml and the answer-file ISO embed the Administrator
# password, so never commit them. They are regenerated by Terraform on apply.
**/build/
*.iso

# --- OS / editor noise ---
.DS_Store
Thumbs.db
